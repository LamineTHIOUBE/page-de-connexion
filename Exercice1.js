let a= parseInt(prompt("Entrez la valeur de a:"));
let b= parseInt(prompt("Entrez la valeur de b:"));

if ( b != 0) {
    alert ("le quotient est " + Math.floor(a/b));
    alert ("le reste est " + (a%b));
    alert("le ratio est " + (a/b));
}

